export interface CardCollection {
    title: string;
    description: string;
    bgurl?: string;
    icon?: React.ReactNode;
  }
  
  export interface GameCardCollection {
    users: number;
    bgurl: string;
  }
  